import { getPublicPath } from '@/utils/public.utils';

export const IMAGES = {
	LOGOS: {
		MAIN: getPublicPath('assets/logos/logo.svg'),
	},
	CTA: {
		HAMBURGER: getPublicPath('assets/cta/hamburger.svg'),
	},
};
